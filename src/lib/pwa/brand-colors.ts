import { PORCELAIN, SIGNAL_BLUE } from "@/lib/branding/colors";

/** PWA manifest and splash colors — aligned with globals.css tokens. */
export const PWA_BACKGROUND_COLOR = PORCELAIN;
export const PWA_THEME_COLOR = SIGNAL_BLUE;
export const PWA_ACCENT_HOVER = SIGNAL_BLUE;
